import {FC} from "react";
import classNames from "classnames";

const Footer:FC = () => {
    return(
        <div className={classNames('', 'w-2/3 mx-auto')}>footer</div>
    )
}

export default Footer